<h1><%=@title%></h1>
<ul class="users">
  <% @friends.each do |friend| %>
      <li>
        <%= gravatar_for friend, :size => 30 %>
        <%= link_to friend.name, friend %> signed up <%= friend.created_at.to_date %>
      </li>
  <% end %>
</ul>